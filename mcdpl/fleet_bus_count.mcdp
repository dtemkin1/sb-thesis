dp {
    provides available_C [Nat]
    provides available_B [Nat]
    provides available_BWC [Nat]
    provides available_WC [Nat]

    requires capital_cost [USD]

    implemented-by yaml resource("fleet_bus_count.dpc.yaml")
}
